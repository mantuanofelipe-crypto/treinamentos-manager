@page "{id}"
@model TreinamentosManager.Pages.Instrutores.DeleteModel
@{
    ViewData["Title"] = "Excluir Instrutor";
}

<div class="card shadow-sm">
    <div class="card-header-dk d-flex align-items-center gap-2">
        <i class="fas fa-trash"></i>
        <h5 class="mb-0">Excluir Instrutor</h5>
    </div>
    <div class="card-body p-4">
        @if (!string.IsNullOrWhiteSpace(Model.Erro))
        {
            <div class="alert alert-warning">@Model.Erro</div>
        }
        else
        {
            <div class="alert alert-danger">Tem certeza que deseja excluir este instrutor?</div>
        }

        <dl class="row">
            <dt class="col-sm-3">Nome</dt>
            <dd class="col-sm-9">@Model.Instrutor.Nome</dd>
            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9">@Model.Instrutor.Email</dd>
            <dt class="col-sm-3">Turmas vinculadas</dt>
            <dd class="col-sm-9">@Model.TotalTurmas</dd>
        </dl>

        <div class="section-divider"></div>
        <form method="post" class="d-flex gap-2">
            <input type="hidden" asp-for="Instrutor.Id" />
            <button type="submit" class="btn btn-danger btn-lg px-4" disabled="@(Model.TotalTurmas > 0 ? "disabled" : null)">
                <i class="fas fa-trash me-1"></i> Excluir
            </button>
            <a asp-page="./Index" class="btn btn-dk-secondary btn-lg px-4">
                <i class="fas fa-arrow-left me-1"></i> Voltar
            </a>
        </form>
    </div>
</div>
